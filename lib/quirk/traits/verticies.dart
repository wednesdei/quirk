/* 
var vertices = Vertices(VertexMode.triangleStrip, [
      Offset(100, 100),
      Offset(200, 100),
      Offset(100, 200),
      Offset(200, 100),
      Offset(200, 200),
      //Offset(700, 200),
    ]);
    canvas.drawVertices(
      vertices,
      BlendMode.plus,
      Paint()..color = Colors.blue,
    );

    */